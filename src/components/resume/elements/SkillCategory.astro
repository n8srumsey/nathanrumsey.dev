---
interface Props {
  category: SkillsData
}
const { category } = Astro.props;
---

<div class="flex gap-4 text-sm">
    <span class="font-mono text-muted-foreground w-28 shrink-0">
        {category.name}
    </span>
    <span>{category.items.join(", ")}</span>
</div>