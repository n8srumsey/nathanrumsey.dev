---
import ExperienceEntry from "./elements/ExperienceEntry.astro"

interface Props {
    experience: ExperienceData[]
}
const { experience } = Astro.props
---
<section class="mt-10">
    <h2 class="text-base font-semibold uppercase tracking-wide text-muted-foreground border-b border-border pb-1 mb-5">Experience</h2>
    <div class="space-y-8">{experience.map((exp) => (<ExperienceEntry experience={exp}/>))}</div>
  </section>